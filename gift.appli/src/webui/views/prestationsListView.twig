{% extends 'base.html.twig' %}

{% block title %}Toutes les prestations{% endblock %}

{% block content %}
    <h1>Liste des prestations</h1>
    <div class="prestations-grid">
        {% for p in prestations %}
            <div class="prestation-card">
                <a href="{{ url_for('prestation', {id: p.id}) }}">
                    <img src="{{ img_dir }}/{{ p.img }}" alt="{{ p.libelle }}">
                    <div class="prestation-card-body">
                        <h3>{{ p.libelle }}</h3>
                        <p class="prestation-prix">{{ p.tarif }} € / {{ p.unite }}</p>
                        <p class="prestation-cat">{{ p.categorie.libelle }}</p>
                    </div>
                </a>
                {% if current_box_id and current_box_statut == 1 %}
                    <form action="{{ url_for('addPrestation', {id: p.id}) }}" method="post">
                        <button type="submit">Ajouter à ma box</button>
                    </form>
                {% endif %}
            </div>
        {% else %}
            <p>Aucune prestation disponible.</p>
        {% endfor %}
    </div>
{% endblock %}
